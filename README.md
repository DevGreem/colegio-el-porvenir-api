## Como ejecutar

Este proyecto usa laravel.



Decargar dependencias:

```bash
composer install

# Ejecutar tambien:
php artisan install
```


Comando para ejecutar:

```bash
php artisan serve
```


## Base de datos

Este proyecto usa Supabase como base de datos, por lo que no es necesario hacer alguna configuración ya que funciona en la nube.

(Si no funciona la conexion, de seguro es por que la DB fue pausada al ser gratuita, avisar)
